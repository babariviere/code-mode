# Pi host

Explicitly adapts selected Pi tools into capability namespaces.

This package accepts already-adapted Pi tool callbacks. It does not discover or load the Pi SDK,
ACP, or extension directory; the host integration is responsible for supplying those callbacks.
